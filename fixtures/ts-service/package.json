{
  "name": "@autopilot/fixture-ts-service",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
